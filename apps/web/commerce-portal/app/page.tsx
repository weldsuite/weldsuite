export default function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8 text-center text-muted-foreground">
      <p>Open /{'{workspace-slug}'} to sign in to the order portal.</p>
    </main>
  );
}
