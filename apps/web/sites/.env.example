# WeldSuite Sites API Configuration

# Sites API URL (used by server-side requests)
# Use internal URL if running in same network, external URL otherwise
SITES_API_URL=http://localhost:5000

# Public API URL (fallback for client-side, though we use server actions)
NEXT_PUBLIC_API_URL=http://localhost:5000
