import { createFileRoute } from '@tanstack/react-router';
import PageComponent from '@/app/weldconnect/variables/page';

export const Route = createFileRoute('/weldconnect/variables/')({
  component: PageComponent,
});
