import { type Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Errors',
  description:
    'Understand error responses from the WeldSuite API and how to handle them.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
