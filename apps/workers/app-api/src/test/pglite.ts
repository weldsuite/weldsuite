// Moved to @weldsuite/worker-kit/testing/pglite so module workers share it;
// this re-export keeps existing imports working.
export { createPgliteDb, isPgliteAvailable } from '@weldsuite/worker-kit/testing/pglite';
