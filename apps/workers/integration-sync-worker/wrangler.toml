# Integration Sync Scheduler Worker
#
# Cron is parked: the previous */15 sweep opened every tenant Neon DB to check
# CRM due-ness (and ran D1 connector catch-up on the same tick). Re-enable only
# after a due-index rewrite that opens a tenant when a row is actually due.
# Manual sync via app-api is unaffected.

name = "integration-sync-scheduler"
main = "src/index.ts"
compatibility_date = "2024-12-30"
compatibility_flags = ["nodejs_compat"]

[triggers]
crons = []

[vars]
ENVIRONMENT = "development"

[[hyperdrive]]
binding = "HYPERDRIVE_MASTER"
id = "f1b2e092e6b64ade8ea7e7e17a1a0d39"
localConnectionString = "postgresql://postgres:__LOCAL_DEV_PASSWORD__@localhost:5432/weldsuite_master?sslmode=require"

# app-api service binding. Calls POST /api/integrations/connections/:id/sync and
# /renew-watch on the internal router (app-api routes/integrations/internal.ts),
# which is mounted ABOVE the Clerk guard and authenticates on X-Internal-Secret.
# This worker's INTERNAL_API_SECRET must match the target app-api env's secret —
# the router fails closed with 401 otherwise.
[[services]]
binding = "APP_API"
service = "weldsuite-app-api"

# D1 connector catch-up index — shared with app-api. The sweep probes store
# APIs from rows here and only calls app-api when the remote has new data.
# DATABASE_ENCRYPTION_KEY / DATABASE_ENCRYPTION_KEY_V2 must match app-api
# (wrangler secret put) so probes can decrypt the D1 credential copy.
[[d1_databases]]
binding = "CONNECTOR_SYNC_INDEX"
database_name = "weldsuite-connector-sync-index-test"
database_id = "5d4f220f-a1cc-4ec0-9e4c-dbaca5647370"

# ============ TEST ENVIRONMENT ============
[env.test]
name = "integration-sync-scheduler-test"

[env.test.vars]
ENVIRONMENT = "test"

[[env.test.services]]
binding = "APP_API"
service = "weldsuite-app-api-test"

[[env.test.d1_databases]]
binding = "CONNECTOR_SYNC_INDEX"
database_name = "weldsuite-connector-sync-index-test"
database_id = "5d4f220f-a1cc-4ec0-9e4c-dbaca5647370"

[env.test.triggers]
crons = []

[env.test.observability]
enabled = true
head_sampling_rate = 1

# ============ PRODUCTION ENVIRONMENT ============
[env.production]
name = "integration-sync-scheduler-production"

[env.production.vars]
ENVIRONMENT = "production"

[[env.production.hyperdrive]]
binding = "HYPERDRIVE_MASTER"
id = "2cdec4bef19746d1a2c2e9726ddaacc3"

# app-api's [env.production] name is "weldsuite-app-api" (no "-production" suffix).
[[env.production.services]]
binding = "APP_API"
service = "weldsuite-app-api"

[[env.production.d1_databases]]
binding = "CONNECTOR_SYNC_INDEX"
database_name = "weldsuite-connector-sync-index-production"
database_id = "bcf01cc5-c2a0-4a29-9881-ecb380e94832"

[env.production.triggers]
crons = []

[observability]
enabled = true
head_sampling_rate = 1

[env.production.observability]
enabled = true
head_sampling_rate = 1
