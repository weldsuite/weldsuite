# External API Environment Variables

# Server port
PORT=3020

# Master database connection (required for @weldsuite/db)
MASTER_DATABASE_URL=postgres://user:password@localhost:5432/master

# Tenant database connection (for workspace-scoped queries)
DATABASE_URL=postgres://user:password@localhost:5432/tenant
