// Component-test mount entry. Intentionally minimal — the host-control
// assertions key off conditional *rendering* (which menu items / rows exist),
// not Tailwind styling, so we don't pull the full Tailwind pipeline in here.
